{
  "sourceId": "stripe-source-uuid-12345",
  "destinationId": "s3-destination-uuid-12345",
  "namespaceDefinition": "destination",
  "prefix": "stripe_",
  "syncCatalog": {
    "streams": [
      {
        "stream": {
          "name": "charges",
          "jsonSchema": {},
          "supportedSyncModes": ["incremental", "full_refresh"]
        },
        "config": {
          "syncMode": "incremental",
          "cursorField": ["created"],
          "destinationSyncMode": "append"
        }
      },
      {
        "stream": {
          "name": "refunds",
          "jsonSchema": {},
          "supportedSyncModes": ["incremental", "full_refresh"]
        },
        "config": {
          "syncMode": "incremental",
          "cursorField": ["created"],
          "destinationSyncMode": "append"
        }
      }
    ]
  },
  "schedule": {
    "units": 1,
    "timeUnit": "days"
  }
}